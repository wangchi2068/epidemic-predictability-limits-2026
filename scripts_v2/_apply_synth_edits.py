# -*- coding: utf-8 -*-
"""Apply the limitations + conclusion rewrite to paper_synth/main.tex.

Uses short unique anchors and asserts each one, so a silent no-op is
impossible.  Kept as a file (not a heredoc) because bash mangles backslashes.
"""
import io
import sys

sys.stdout.reconfigure(encoding="utf-8", errors="backslashreplace")
P = "paper_synth/main.tex"
s = io.open(P, encoding="utf-8").read()

BS = chr(92)
ref = BS + "ref{sec:phase_collapse}"
kagg = "$k_{" + BS + "text{agg}}$"

EDITS = [
    (
        "本文的适用边界体现在六个方面。",
        "本文的适用边界体现在七个方面。",
    ),
    (
        "而第~" + ref + " 节的证据恰指向峰前期的动态方差膨胀——若方差确在峰前膨胀，固定 "
        + kagg + " 的 NB2 在最需预警的时段即非正确工作模型；时变离散度下的视界重估是本文未完成的后续工作。",
        "而第~" + ref + " 节的分解诊断表明峰前失效以位置偏差为主导、宽度不是瓶颈，"
        "故时变离散度" + BS + "textbf{不是}恢复峰前校准的主要方向；真正待答的是均值模型在转折期的系统性方向偏差。"
        "新增两条边界：(i)~" + BS + "textbf{分布性而非逐点成立}：294 个逐州配对单元中 $21.1" + BS + "%$ 的机制视界短于实测时效，"
        "其中流感 2022--23 与 RSV 2025--26 的反号比例达 $31.6" + BS + "%$ 与 $38.5" + BS + "%$，"
        "故本文不将机制视界表述为外包络，只表述为约五分之四单元上同向的诊断性参照；"
        "(ii)~" + BS + "textbf{oracle 位置缩放是上界诊断}：它证明位置修好即足以恢复覆盖，但不证明位置修好就能做到；"
        "可部署的相位敏感均值模型能否在样本外复现该增益，是本文未完成的后续工作。",
    ),
    (
        "可核对的主要结论有四：",
        "可核对的主要结论有五：",
    ),
    (
        "其四，FluSight 审计中机制预测器全相位欠覆盖，峰前期额外击穿与静态方差设定偏误不可分离，相位分层只能提供描述性线索。",
        "其四，FluSight 审计中机制预测器全相位欠覆盖（全季 0.405--0.554），"
        "且其与实测时效的关系是分布性的——294 个逐州配对单元中 $78.9" + BS + "%$ 同向、$21.1" + BS + "%$ 反号，故不构成逐点外包络；"
        "其五，也是本文的新发现，" + BS + "textbf{峰前校准失效以位置偏差为主导}："
        "峰前单元 $91.7" + BS + "%$" + BS + "text{--}$100" + BS + "%$ 被低估而峰后系统性转为高估（符号结构跨三季一致），"
        "且把 $k_{" + BS + "text{agg}}$ 放宽至 $1/512$ 仍不能使 v1.1.0/v1.2.0 峰前覆盖率过 0.90，而位置缩放可以——"
        "据此本文将静态方差设定偏误从峰前击穿的主要可归因来源中降级。",
    ),
    (
        "本文未能回答的问题同样明确：动态或时变离散度工作模型能在多大程度上恢复峰前期校准，需要受控的时变 $k_{"
        + BS + "text{agg}}$ 实验；三个赛季、单一国家的证据外推边界如何，需要跨病原、跨体制的扩展审计；"
        "相位分层规则的在线化（例如基于实时增长率符号的构造）尚待实现。机制视界作为同口径下的宽松经验参考外包络，"
        "为公共卫生应急调度提供了统计可信窗口的保守参照，也为下一代将方差设定作为一等公民的动态预测系统标定了改进方向。",
        "本文未能回答的问题同样明确：一个可部署的相位敏感均值模型能否在" + BS + "textbf{样本外}"
        "复现 oracle 位置缩放所指示的增益，需要以前两季估形式、第三季不调参验证的留出实验；"
        "三个赛季、单一国家的证据外推边界如何，需要跨病原、跨体制的扩展审计；"
        "相位分层规则的在线化（例如基于实时增长率符号的构造）尚待实现。"
        "机制视界作为同口径下的诊断性经验参照，为公共卫生应急调度提供了统计可信窗口的保守边界，"
        "也为下一代把方差设定与均值动态同时作为一等公民的预测系统标定了改进方向。",
    ),
]

applied = 0
for old, new in EDITS:
    c = s.count(old)
    assert c == 1, f"anchor count {c} for: {old[:60]!r}"
    s = s.replace(old, new)
    applied += 1

io.open(P, "w", encoding="utf-8", newline="").write(s)
print("applied", applied, "edits")
