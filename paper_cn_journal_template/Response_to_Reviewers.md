# Response to Reviewers — Round 13

**Manuscript:** 传染病动力学可预测视界、数理极限与实证分析 (Predictability horizons, mathematical limits, and empirical analysis of infectious-disease dynamics)

**Revision base:** commit `4035821` → this revision (peer-review report 13, 19 Major + 18 Minor findings).

We are grateful for an audit of unusual depth and fairness. The review's central structural diagnosis was correct: the previous "single source of truth" mechanism validated a file (`reports/table_bodies.tex`) that never entered the compilation, so its net coverage of the printed tables was zero, and two data streams — hand-typed manuscript and generated artifacts — had run in parallel without reconciliation. This revision attacks that root cause first and repairs the numerical, disclosure, and theoretical items behind it. Following the review's own suggestion (Minor 18), **this letter cites locations by section number, table/theorem number, and environment name rather than line numbers**, because line numbers drift; every cited location is verifiable in the committed source.

**Verification of this letter's own claims.** The build is now genuinely one command: `python scripts_v2/make_all.py` (pipeline → rolling → error accounting → verification suites → Figure-3 simulator → figures → table bodies → consistency tests). The rewritten `check_consistency.py` fails the build unless (i) `main.tex` `\input`s all five generated table fragments, (ii) each fragment equals the JSON-recomputed body byte-for-byte, (iii) every referenced figure exists and is non-blank (pixel-content check), (iv) the abstract headline range equals the JSON extremes, (v) a 48-item stale-literal blacklist is absent from the compiled source, and (vi) the corrected Cramér–Rao ratio is reproduced. All checks pass; the PDF compiles clean (40 pages, XeTeX chain).

---

## Part I — Point-by-point responses to Major comments

### Major 1 — Dimensional error in `E_drift` (code squared an already-squared variance)
**Fixed.** `scripts_v2/error_budget_v2.py`: `e_drift = h_gen * v_drift` (was `h_gen * v_drift ** 2`), matching the printed formula $\mathcal{E}_{\text{drift}}(h) = h_{\text{gen}} \cdot \hat{v}^2$ in Section 4.4. The variable was renamed in the docstring to state that `drift_volatility()` returns a variance. The chain was rerun; Table 4, Figure 6, the Chinese abstract, the English abstract, and the conclusion were all regenerated from the corrected `table4_budget.json`. As promised in the text, the fix exposes negative closure residuals at three short-horizon configurations (influenza $h=1$: $-54.2\%$; RSV 2024–25 $h=2$: $-297.4\%$; RSV 2025–26 $h=2$: $-138.1\%$); these are now printed unclipped in Table 4 and interpreted as a diagnostic that the 16-week drift-calibration band is too wide at low-count short-horizon working points. Section 4.4 and the Table 4 note state this explicitly.

### Major 2 — Printed Table 4 was not the JSON's table
**Fixed via Majors 1 + 5.** The row set now comes from `error_budget_v2.py` (Delta/Omicron/flu22 at $h=1,2,4$; rsv24 at $h=2,4$; rsv25 at $h=2$), the P and CV² columns are computed from Table 2's stored parameters through one code path, and the compiled table is the generated fragment (see Major 5). The old abstract range 51.2%–99.6% and the printed-table range 34.6%–95.4% are both retired: they were, respectively, a buggy-JSON artifact and a transcription artifact. The corrected headline, now identical across abstract, Figure 6 caption, English abstract, and conclusion, is: unattributed structural residuals are positive in 9 of the 12 configurations, spanning 6.2%–99.2% and exceeding 40% in 8 of those 9, with three negative rows reported as diagnostics rather than suppressed.

### Major 3 — P column low by exactly 10× in nine rows
**Fixed.** The transcription error is structurally eliminated: the P column of the compiled Table 4 is now `\input` from `tables/table4_body.tex`, generated directly from `table4_budget.json`'s `p_param` field, and the consistency test compares fragment to JSON on every build. Values match Table 2 recomputation (e.g., Delta $h=1$: $4.0\times10^{-4}$; Omicron $h=4$: $468.9\times10^{-4}$).

### Major 4 — Three incompatible definitions of $I_0$
**Fixed by choosing and documenting one definition per quantity.** Section 4.4 now states explicitly: the theoretical CV² term uses the phase-level window-mean $I_0$ from Table 2, while the observed $\text{relMSE}^2_{\text{obs}}$ normalizes by the per-origin last-week value $I_{0,t}=Z_t$ — an intentional "phase-level theoretical floor vs. per-origin realized backtest" contrast, not a hidden inconsistency. The contradictory claim that dynamical parameters are "100% taken from Table 2" was rewritten accordingly (Table 4 note). CV² values in the compiled table are recomputable from Table 2 inputs by construction (single generator).

### Major 5 — Single-source mechanism disconnected from the compiled document
**Fixed; this was treated as the precondition for everything else.** `main.tex` now contains `\input{tables/table2_body}` … `table6_body}` inside the `\resizebox` wrappers; every data row of printed Tables 2–6 comes from files emitted by `make_tables_v2.py` from the JSONs. Both `check_consistency.py` table assertions were redirected from the never-compiled `table_bodies.tex` to the compiled fragments, with a byte-equality test between JSON-recomputed bodies and on-disk fragments, plus a test that `\input` statements are present and no hand-typed Table 2 rows survive. One implementation detail is worth recording for the authors' and reviewer's benefit: the 2024+ LaTeX kernel's tabular machinery rejects `\input` *inside* a row stream, so fragments carry complete `tabular` environments (specification + header + rules + rows); this also brought the table headers under single-source control. The `h*_approx`-column bug at `make_tables_v2.py` (approximate column filled with exact values) is fixed: the column now evaluates the first-order closed form (Delta: 37.0/24.9 — matching the previously hand-typed value, as the review predicted).

### Major 6 — Extra R² in the `C_req` implementation
**Fixed.** `pipeline.py::crb_analysis` now computes $C_{\text{req}} = (1/R + 1/k)/s^2$ with `s_rel = s_gen` (s_gen is already the log-scale relative SE per Table 2's note). Recomputed values: rsv25 C_req = 13,785 vs. 9,341 → **ratio 1.48** (was 2.46); rsv24 4,882 vs. 22,047 → 0.22. `crb_analysis()` was moved above `if __name__ == "__main__"` and is now called from `main()`, so `make_all.py` regenerates `crb_analysis.json`; a build assertion ties the JSON ratio to the printed formula. Both abstracts, Section 4.2, and the Table 2 discussion now carry 1.48 and the softer language per Major 7. The qualitative conclusion (data sufficiency in doubt for rsv25's 67.8-week root) survives at 1.48 and is now honestly labeled.

### Major 7 — Cramér–Rao comparison is a category mismatch
**Fixed via the review's minimum remedy, with the model-matched re-estimation declared as remaining work.** The word "violation" is deleted from the abstracts and Section 4.2; the diagnostic is now labeled an order-of-magnitude heuristic. A bridging paragraph in Section 4.2 states exactly what the review demanded: Theorem 4 bounds an unbiased NB-likelihood MLE over $C$ independent clusters, whereas the substituted $s$ is an OLS slope SE from five aggregated points, and weekly-aggregation smoothing inflates C_req through the $s^2$ denominator; the correct reading is "data sufficiency for the 67.8-week root is questionable," not an information-theoretic violation. The corroboration the review noted (ratios spanning ~400× across phases) is addressed by the smoothing caveat. **Not done this round:** re-estimating R on the five-week windows under a negative-binomial likelihood (GLM or renewal MLE) to produce a model-matched SE(R̂); we agree this makes the comparison self-consistent and plan it as the substantive follow-up rather than half-implement it now.

### Major 8 — Figure 3 blank
**Fixed, with root cause confirmed.** The plotting function read `d.get("cv", d)` while the JSON's key is `stationary_cv`, so zero bars matched and the empty figure passed existence/size checks. `make_all_figures_v2.py::gen_fig_t3` was rewritten into two panels — (a) empirical/theory CV ratios with a 1.0 reference line, (b) establishment probability with 95% Beta intervals vs. the Poisson $2\varepsilon/R$ theory — and now **raises if zero data objects are found**. `sim_verify_t3.py` was promoted from `scripts/legacy/` into `scripts_v2/` and wired into `make_all.py` (skipped by `--fast`). The figure, Section 3 bullet, and caption ratios were reconciled to `verify_t3.json`: supercritical 0.9995/0.9991, near-critical 0.976/0.950, Poisson establishment 7/8 groups covered. `check_consistency.py` adds a non-background-pixel fraction test (>5%) for every referenced figure.

### Major 9 — Spectral gap asserted, boundary lost in the limit
**Fixed via the review's second remedy (literature positioning).** Appendix C and the Theorem 3 proof sketch now state the exact objection the review raised: under $I=\sqrt{N}y$ the reflecting boundary maps to $y_{\min}=1/\sqrt{N}\to0$, the limiting CIR-type generator has 0 as a natural entrance/absorbing boundary, and the O(1) gap is therefore *not* established within our construction. The $O(N^{1/2})$ claim is repositioned as an application of the classical relaxation scaling for density-dependent near-critical branching diffusions, cited to Nåsell (1999) and Dolgoarshinnykh & Lalley (2006) — both already in the bibliography and named in Section 1.2 — and the theorem title/prose were downgraded from "证明/demonstrates" to "approximation/application" wording. The abstract now says the scaling is "argued via a diffusion approximation."

### Major 10 — Local-linear baseline read backwards; null crossings undisclosed
**Fixed.** Section 4.4 now states, with numbers from `rolling_results.json`: against the local-linear baseline the skill ratio **decreases** monotonically in all three waves (Delta 3.11→0.71; Omicron 1.06→0.08; flu22 0.95→0.16) — the model is worse than this baseline at short range and better at long range — and `crossing.lin` is **null for all three waves** within $h\le8$; the old sentence implying parallel divergence is deleted. Seasonal-naive crossings computed under the manuscript's own interpolation definition are now reported (Delta 2.78, Omicron 1.71; influenza genuinely unavailable due to the 52-week lag), so the exclusion-by-influenza-only overreach is corrected. The 1.8–5.3-week headline is named as persistence-relative in the abstract, conclusion, Table 5 note, and Figure 7 caption, and the Figure 7 plot (which already draws the dotted linear-baseline curves) is captioned accordingly.

### Major 11 — Theorem 5R does not generalize Theorem 5
**Fixed.** The connecting sentence now states precisely what the review wrote: 5R characterizes only the environmental-noise component of $\sum_j r_{t+j} = \log(Z_{t+h}/Z_t)$, contains no $1/k$ sampling floor, and does **not** reduce to Theorem 5 at $\phi=0$; the complete horizon-relevant variance requires adding Theorem 5's $h/k$ term under an independence assumption. "重要动力学推广" is replaced with "环境噪声分量的平行扩展," the theorem environment's own title now reads "定理 5 环境噪声分量的时间自回归平行扩展," and the object mislabel "累计发病量" was corrected to the sum of log growth rates.

### Major 12 — Bootstrap intervals carry no phase-specific information (df=3 artifact)
**Fixed.** Section 4.2 now explains the mechanism the review derived (CV² ≪ τ² ⇒ h* ≈ τ/s ⇒ interval shape governed by the df=3 χ-type fluctuation of the slope SE, nearly data-free), states the near-constant relative widths (lower/point ≈ 0.55, upper/point ≈ 3.77 across all seven phases), and explicitly forbids cross-phase certainty comparisons from these intervals. The old "随序列长度与信噪比自然离散" explanation is deleted. We did not widen the windows or switch to Bayesian posteriors, to avoid destabilizing the frozen protocol mid-review; the Table 2 note and Section 4.2 carry the limitation instead.

### Major 13 — Extrapolation up to 13.6× the window, natural-span test applied only to RSV
**Fixed.** Table 2 gains an $h^*/W$ column (generated): 1.6× Omicron, 2.2× JN.1/flu22, 3.1× flu24, **4.2× Delta**, 8.3× rsv24, 13.6× rsv25. Section 4.2 now applies the natural-span logic uniformly and concedes the review's point verbatim in substance: Delta's 21.2-week root equals or exceeds the wave's ~19-week life cycle and must be read with the same truncation logic as RSV, notwithstanding the dagger reserve. The abstract flags the 4.2× extrapolation next to the 7.9–21.2 headline and states that operational meaning of h* should be doubted above ~4×.

### Major 14 — MBB on 6 origins/4 blocks; 34.8% silent Omicron resample failures
**Fixed (disclosure) / declared (redesign).** Table 5 gains an $n_{\text{boot}}/1000$ column (975 / 652 / 907) and Section 4.4 discloses the discard mechanism and its direction (failed = better-performing samples, so the retained interval is shifted and narrowed), labels the Omicron interval an indicative range, and states the 4-admissible-block / ≤16-ordered-resample degeneracy. We did not replace the intervals with per-origin listings or run the full block-length sensitivity this round; both are planned, and the text no longer calls these percentile ranges "95% CI" without qualification.

### Major 15 — Precedents uncited (two in the authors' own bib)
**Fixed.** Wesselkamp et al. 2025 and Castro et al. 2020 — both present-but-uncited in `references.bib` — are now cited in Section 1.2 with an explicit increment statement in the review's own terms: the potential/absolute-vs-relative forecast-limit architecture and the verification-data/score/tolerance triad are attributed to Wesselkamp et al.; the narrow-probabilistic-window physical picture is attributed to Castro et al.; and this paper's residual contribution is stated as assembling the two-tier picture into one analytically solvable closed form CV²(h) + P(h) ≤ τ² under NB-branching + lognormal propagation, with seven-phase calibration and a sample-size diagnostic — "a specific analytical instantiation," matching the self-description at the contribution statement, whose "first principles … rigorously derive" wording was also downgraded. Ho et al. 2023 and Němcová et al. 2026 are cited at the k_agg estimation paragraph as methodological comparators. The "66 stored, 38 cited" orphan discrepancy the review flagged is acknowledged; pruning orphan entries from `references.bib` is documentation work we did not want to bundle into a numerical repair commit.

### Major 16 — Abstract claims real-time operational skill from a finalized-vintage backtest
**Fixed.** Chinese abstract: "实际业务预测技能" replaced with persistence-relative, pseudo-prospective wording, and the sentence now states that the finalized-vintage backtest is an upper bound under no reporting-quality loss and that real-time skill can only be shorter; the conclusion repeats the caveat; the English abstract names persistence, finalized vintage, and the upper-bound property; the Table 5 note carries it too.

### Major 17 — Chinese abstract lacked the promised "探索性"
**Fixed.** The Chinese abstract now says 双层**探索性**基准框架 with 提出 (not 确立), and states the sample scope (three waves, six origins) in the abstract itself; the conclusion matches. The letter's previous-round failure to deliver this word is noted in Part III.

### Major 18 — Ceiling-truncated k printed as a point estimate
**Fixed (marker + discussion); partial.** Tables 2 and 3 print `1000.0` with a `\ddagger` marker defined in the notes as a truncation whose correct reading is non-identifiability at that aggregation scale, not an estimate. Section 4.2 now extends the discussion to the Table 3 decline-phase hits and recomputes the sensitivity honestly: the 0.74% CV² share moves from 0.736% to 0.735% as k → ∞ (because R/k ≈ 0.0013 here), so the 0.74% headline is robust despite the ceiling — the review's concern that 0.74% might materially shrink is answered numerically rather than by assertion. **Not done:** reporting the raw pre-truncation second-moment estimate and its SE (the estimator returns the capped value and discards the raw quantity); added to the follow-up list.

### Major 19 — Rounding direction in h*_stoch
**Fixed.** Property 3 of Theorem 1 now uses ⌊·⌋, with a one-line justification (strict monotonicity of CV² ⇒ the largest admissible integer is the floor of the continuous root) and an explicit note that the ceiling would return an h* violating CV²(h*) ≤ τ², i.e., overstate predictability.

---

## Part II — Minor comments

1. **"Orthogonal"** — Six illegitimate usages replaced with additive wording (contribution bullet, structure paragraph, Figure 1 banner, Theorem 1 statement, Theorem 1 proof sketch, conclusion). The three standard usages (law-of-total-variance L² decomposition at Lemma 1; martingale-difference orthogonality at Theorem 5 proof and Appendix E) are retained, as the review itself allows.
2. **Theorem 1 property 1** — Now states the exact condition CV²(1) + P_exact(1) ≤ τ² as necessary-and-sufficient, and labels the displayed s² form a necessary (not sufficient) quick-check criterion by strict convexity, with the <0.2% practical gap at s ≤ 0.04 noted.
3. **σ² three definitions** — Theorem 3 now carries a relationship paragraph: its diffusion coefficient (R+1)I is the near-critical Poisson-type birth–death scale, it **deliberately omits k**, Theorem 3 is a parallel near-critical model rather than a limit of the (A1) branching model (whose variance is R + R²/k), and the susceptible-depletion drift is an exogenous mechanism excluded by the main model; the power-law conclusion is unaffected.
4. **Lemma 2 R>1 qualification** — Moved into the statement (saturation limit asserted only for R>1; divergence noted for R≤1; near-critical extension reads R → 1⁺).
5. **Table 3 CV²-contribution column** — Regenerated under the Major 5 connection: computed by the generator for R>1 rows only, with `--` printed for all R<1 rows (the definition is negative/meaningless there), and the note says so. The previously unreproducible supercritical plateau row (1.34% printed) no longer exists as a hand-typed artifact: every compiled cell now equals its JSON recomputation by build assertion.
6. **Decline-phase roots as horizons** — Retained the $^*$ marker and regime-shift note; values are generator-produced and the 34.5-week hand-typed root (which satisfied no printed equation) is gone — the compiled table now shows the full-variance root for the flu22 decline window (11.1 weeks). Column header and note clarify subcritical rows are mathematical roots, not lead times.
7. **Cross-scale equivalence** — Restated as a conditional numerical-negligibility claim: exact for P(h), <0.01% for the Table 2 rows, O(10%) possible where CV² is not negligible, with the k-scale-dependence caveat; the "exactly the same" phrase and the Table 2 note's "strictly equivalent" are deleted.
8. **Unbiased vs lognormal estimator** — Unified on the lognormal model: Figure 1 now labels log R̂ ~ N(log R, s²); Theorem 2's proof parenthetical is replaced by the exact statement E[Y] = e^{s²/2} with the O(s²) bias absorbed by the full-MSE definition of P(h); a sentence at the Theorem 4 / Section 4.2 junction states the bound applies to unbiased MLEs in its own setting and is not asserted of the sliding-window estimator.
9. **Theorem 5 conditioning unused** — Proof sketch now adds the explicit independence assumption for {η_t}, {ξ_t} and explains the conditioning treatment: at E[Z_h] ≫ 1 the conditional and unconditional leading-order variances coincide (extinction probability decays geometrically), so the unconditional computation gives the stated asymptotic; finer change-of-measure handling is explicitly declared out of scope rather than glossed. Appendix E keeps the regularized log and now flags the same convention.
10. **Corollary 3** — Symbol split ($\hat I$ single-generation estimate vs. $\tilde I_h$ cumulative), the h-fold accumulation tied to Theorem 5's log-increment decomposition with its independence premise, and "下界/边界" replaced with "一阶渐近累积律 (∼ 意义)" in statement, proof, and Appendix E; Lemma 2's title likewise changed from "方差下界" to "渐近饱和极限" (it is an equality).
11. **Table 6 τ=0.50 column vs Table 2** — Fixed at the generator: `table6_rows` reuses Table 2's stored `h_star_weeks` verbatim for τ=0.50 and solves only the other three levels; row-for-row identity now holds (Delta 21.2, flu24 15.7, rsv24 41.5, rsv25 67.8), the non-RSV summary row reads 7.9–21.2 matching the abstract, and the scenario paragraph changed 21.3→21.2. The sublinearity claim was re-derived from the corrected roots: 3.5× tolerance relaxation yields 2.82–2.87× horizon growth (old text said 2.72–2.92).
12. **Four vs five weeks** — Section 4.1 now says W=5 (five weekly points, df=3) for Table 2 estimation and explicitly contrasts the W=4 rolling window used by the error accounting and rolling evaluation, with each table's note stating its own window.
13. **Figure 2/6 captions** — Figure 2's caption now matches the actual plot (blue solid Lemma 2 curve, red 20,000-path scatter, grey dashed CV²∞; axes CV²(h) vs h). Figure 6's caption describes the new rendering (blue/orange/green stacked modeled shares vs red 100% dashed observed line) and no longer claims colors or a term name that the figure does not show.
14. **New overstatements** — "100% 严密自洽" deleted (replaced by the Major 4 wording); "完全实测" removed at all five locations in favor of descriptive accounting; Corollary 1's "高解析保真" qualified per Minor 16.
15. **第一性原理** — Zero occurrences now (contribution bullet, Section 5.2 cluster-mapping sentence, and conclusion opening all reworded to model-scoped or statistical-inference framing). The blacklist in `check_consistency.py` now asserts this permanently.
16. **Corollary 1 full-domain error** — 48.6% (at the |ε|=0.2, I₀=50 corner) is now printed in both the proof sketch and Appendix B alongside the narrow-band 15.8%, and both texts state that empirical results use the exact numerical root exclusively. A new clarifying sentence separates the first-order supercritical closed form (16.5–17.6% bias over the seven phases, Section 4.2) from the near-critical quadratic root (grid error in Appendix B) — the review's own initial conflation of the two is evidence the distinction needed to be explicit.
17. **Bibliographic errors** — `parag2026threshold` pages 45→185; Petchey accent repaired (`K{'e}fi` → `K{\'e}fi`); `chan2026estimating` given article number 16890; the systematic stray-closing-brace defect throughout `references.bib` (which had corrupted parsing) was repaired by rebuilding every entry with a strict parser. **Rejoinder on the "duplicate keys":** `lloyd-smith2005superspreading` (Nature 438:355–359, superspreading) and `lloydsmith2005should` (TREE 20:511–519, population thresholds for wildlife disease) are two genuinely distinct Lloyd-Smith 2005 papers, not a duplicate; no merge was performed. **Not done:** the three missing DOIs (oregan/salinas/southall) were not added rather than filled from memory; recorded for the next round.
18. **Line numbers in the response letter** — This letter cites by section/table/theorem name; the previous letter's line-number convention is retired.

---

## Part III — Honest accounting of limitations and residual gaps

**Not claimed fixed:**

- **Major 7 (best remedy):** no negative-binomial-likelihood re-estimation of R̂ on the inference windows this round; the comparison is repaired at the wording/bridging level and quantitatively corrected (1.48), but the model-matched estimator is future work.
- **Major 12/14 (redesign level):** window widening / Bayesian propagation of (k, I₀), block-length sensitivity analysis, and per-origin crossing listings are disclosed as limitations rather than implemented; the df=3 and ≤16-resample degeneracies are now stated in-text.
- **Major 18 (partial):** raw pre-truncation second-moment values and SEs are not reported because the estimator does not currently return them.
- **Major 3 legacy:** byte-exact raw-vintage reconstruction and prospective multi-season evaluation remain declared limitations (unchanged from last round).
- **Minor 17 (partial):** three DOIs left blank rather than guessed.
- **Reference hygiene:** 28 uncited `references.bib` entries (now including four newly-cited ones ⇒ 24 remaining orphans) are untouched.

**Self-reported process note.** During revision one scripted edit corrupted the manuscript source; it was restored from the commit the review audited (`4035821`) and every change was re-applied and re-verified through the build, the consistency suite, and the compiler, all of which pass. The review's finding that automated checks once validated the wrong file is precisely why, post-fix, the checks validate only compiled content.

**One factual note for the record.** The review's own limitations section states it verified all 30 cited DOIs; our rebuild confirms those resolutions, and we have adopted DOI verification (Crossref) as a pre-submission checklist item for any new entry (the four citations added this round were already verified in the review's reference list).

We believe the manuscript now satisfies the precondition the review set for acceptance-grade revision: printed numbers are generated numbers, generated numbers are recomputable, and the headline claims no longer exceed the body's qualifications.
